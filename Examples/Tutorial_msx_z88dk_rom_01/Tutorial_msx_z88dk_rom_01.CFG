# z88dk ROM tutorial metadata
#
# Build target:
#   +msx -subtype=rom (cartridge ROM build)
# Output:
#   16KB bank-aligned MSX ROM image with AB header
# Note:
#   Choose subtype (rom/rom2) based on required ROM layout.
