# mplayer-z88dk-stub — placeholder Makefile.
# Like Library/MSX/spman-z88dk, examples include the stub source files
# directly in their compile.sh SRCS (zcc full-app build path), so no
# separate .lib is built here.
all:
	@echo "mplayer-z88dk-stub uses direct source inclusion."
	@echo "Headers: $(CURDIR)/include"
	@echo "Source:  $(CURDIR)/src/mplayer/{mplayer_stub.c,song_data_stub.c}"
clean:
	@true
.PHONY: all clean
